{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GrupoProcesos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombregrupo</th>
                <td>{{ entity.nombreGrupo }}</td>
            </tr>
            <tr>
                <th>Porcentaje</th>
                <td>{{ entity.porcentaje }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('grupoprocesos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('grupoprocesos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
