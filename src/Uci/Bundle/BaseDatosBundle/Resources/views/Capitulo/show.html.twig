{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Capitulo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numerocapitulo</th>
                <td>{{ entity.numeroCapitulo }}</td>
            </tr>
            <tr>
                <th>Nombrecapitulo</th>
                <td>{{ entity.nombreCapitulo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('capitulo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('capitulo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
