{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Capitulo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numerocapitulo</th>
                <th>Nombrecapitulo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('capitulo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numeroCapitulo }}</td>
                <td>{{ entity.nombreCapitulo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('capitulo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('capitulo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('capitulo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
