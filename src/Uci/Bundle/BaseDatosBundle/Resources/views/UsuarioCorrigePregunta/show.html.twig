{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UsuarioCorrigePregunta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechacorreccion</th>
                <td>{{ entity.fechaCorreccion|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Comentario</th>
                <td>{{ entity.comentario }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('usuariocorrigepregunta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('usuariocorrigepregunta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
