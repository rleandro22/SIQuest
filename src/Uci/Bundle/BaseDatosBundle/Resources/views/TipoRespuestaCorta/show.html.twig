{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoRespuestaCorta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Correcta</th>
                <td>{{ entity.correcta }}</td>
            </tr>
            <tr>
                <th>Porcentajecorreccion</th>
                <td>{{ entity.porcentajeCorreccion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tiporespuestacorta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tiporespuestacorta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
