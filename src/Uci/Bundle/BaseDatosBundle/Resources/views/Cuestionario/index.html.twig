{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cuestionario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pruebanumero</th>
                <th>Fechaaplicacuestionario</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cuestionario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.pruebaNumero }}</td>
                <td>{% if entity.fechaAplicaCuestionario %}{{ entity.fechaAplicaCuestionario|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cuestionario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cuestionario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cuestionario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
