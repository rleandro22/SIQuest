{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CuestionarioTienePreguntas</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Ordenencuestionario</th>
                <td>{{ entity.ordenEnCuestionario }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cuestionariotienepreguntas') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cuestionariotienepreguntas_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
