{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AreaConocimiento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrearea</th>
                <td>{{ entity.nombreArea }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('areaconocimiento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('areaconocimiento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
