{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoVerdaderoFalso</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Verdadera</th>
                <td>{{ entity.verdadera }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoverdaderofalso') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipoverdaderofalso_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
