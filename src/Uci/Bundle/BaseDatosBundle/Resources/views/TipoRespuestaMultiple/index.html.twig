{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoRespuestaMultiple list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Correcta</th>
                <th>Porcentajecorreccion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tiporespuestamultiple_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.correcta }}</td>
                <td>{{ entity.porcentajeCorreccion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tiporespuestamultiple_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tiporespuestamultiple_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tiporespuestamultiple_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
